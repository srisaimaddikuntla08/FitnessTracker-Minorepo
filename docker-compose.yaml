version: "3.9"

services:
  fitness-tracker:
    image: fitness-tracker:latest
    container_name: fitness-tracker
    ports:
      - "5000:5000"
    env_file:
      - .env
    restart: unless-stopped
